<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= quiz.title %></title>
</head>
<body>
    <h1><%= quiz.title %></h1>
    <!-- <p><%= quiz.quiz_description %></p> -->
    <% if (quiz.questions && quiz.questions.length > 0) { %>
        <!-- <ol>
            <% quiz.questions.forEach(question => { %>
                <li>
                    <%= question.question %>
                    <ul>
                        <% question.options.forEach(option => { %>
                            <li>
                                <%= option.choices %>
                            </li>
                        <% }) %>
                    </ul>
                </li>
            <% }) %>
        </ol> -->
    <% } else { %>
        <p>No questions found for this quiz.</p>
    <% } %>
    <button onclick="location.href='/quiz/<%= quiz.id %>'">Take Quiz</button>


</body>
</html>

